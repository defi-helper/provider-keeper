{
  "name": "provider-keeper",
  "version": "0.0.6",
  "scripts": {
    "provider:test": "npm run test -workspace=provider",
    "provider:coverage": "npm run coverage -workspace=provider",
    "provider:build": "npm run build -workspace=provider",
    "ui:build": "npm run build -workspace=test-app",
    "ui:start": "npm run start -workspace=test-app"
  },
  "workspaces": [
    "provider",
    "test-app"
  ],
  "engineStrict": true,
  "engines": {
    "node": "^16",
    "npm": "^7"
  }
}
